<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-3">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('homepage') }}">Symshop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                {%  for c in categoryRepository.findAll() %}
                    <li class="nav-item"><a style="color: white;" class="me-4  nav-link" href="{{ path('product_category', {'slug': c.slug }) }}">{{ c.name }}</a></li>
                {% endfor %}
            </ul>
           <ul class="navbar-nav">
               <li class="nav-item"><a href="{{ path('cart_show') }}" class="nav-link"><i class="fas fa-shopping-cart"></i> Panier {{ cartService.total }}</a></li>
               {% if app.user %}
                    <li class="nav-item"><a href="{{ path('purchase_index') }}" class="btn btn-warning">Mes Commandes</a></li>
                    <li class="nav-item"><a href="{{ path('app_logout') }}" class="btn btn-warning">Logout</a></li>
               {% else %}
                   <li class="nav-item"><a href="{{ path('app_register') }}" class="nav-link ">Inscription</a></li>
                   <li class="nav-item"><a href="{{ path('app_login') }}" class="btn btn-success">Login</a></li>
               {% endif %}
           </ul>
        </div>
    </div>
</nav>