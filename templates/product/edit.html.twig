{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editer le produit</h1>

    {{ form_start(formView) }}

    {{form_errors(formView) }}

    {# {{ form_widget(formView) }} #}

    {# {{ form_label(formView.name) }}
     {{ form_errors(formView.name) }}
     {{ form_widget(formView.name) }}
     {{ form_help(formView.name) }} #}

    <div class="row">
        <div class="col">
            {{ form_row(formView.name)}}
            {{ form_row(formView.price) }}
        </div>
        <div class="col">
            {{ form_row(formView.mainPicture) }}
            {{ form_row(formView.category) }}
            {{ form_row(formView.description) }}
        </div>
    </div>

    <button class="btn btn-primary" type="submit">
        <i class="fas fa-save"></i>
        Créer le produit
    </button>

    {{ form_end(formView) }}

{% endblock %}
{% block title %} Editer le produit {{ product.name }}{% endblock %}