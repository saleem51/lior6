{% extends 'base.html.twig' %}

{% block body %}
    <h1>Catégorie : {{ category.name }}</h1>
    <div class="row">
        {% for produit in category.products %}
            <div class="col-3">
              {{ include('product/_product_card.html.twig') }}
            </div>
        {% endfor %}
    </div>
{% endblock %}
{% block title %}Catégorie {{ category.name }}{% endblock %}